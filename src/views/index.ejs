<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>Home</title>
    <link rel="stylesheet" href="/styles.css" />

  </head>
  <body>
    <header>
      <h1>Student Feedback Portal</h1>
      <nav>
        <a href="/">Home</a> |
        <a href="/posts">Posts</a> |
        <a href="/search">Search</a> |
        <% if (currentUser) { %>
          <span>Logged in as <%= currentUser.username %></span> |
          <a href="/logout">Logout</a>
          <a href="/profile">Profile</a>
        <% } else { %>
          <a href="/register">Register</a> |
          <a href="/login">Login</a>
        <% } %>
      </nav>
      <hr />
    </header>

    <main>
      <p>Welcome to the Student Feedback Portal.</p>

      <% if (currentUser) { %>
        <p>You are logged in as <strong><%= currentUser.username %></strong>.</p>
      <% } else { %>
        <p>Please <a href="/register">register</a> or <a href="/login">login</a>.</p>
        <p>This is strong secure website, database cannot be injected for example you can try SQL injection. <Strong><code><em><h2>' OR '1'='1</h2></em></code></Strong> or you can use <Strong><code><em><h2>'; DROP TABLE users; --</h2></em></code></Strong> and it will not work.</p>
      <% } %>
    </main>
  </body>
</html>
