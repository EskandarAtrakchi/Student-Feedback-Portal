<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <header>
      <h1>Student Feedback Portal</h1>
      <nav>
        <a href="/">Home</a> |
        <a href="/posts">Posts</a> |
        <a href="/search">Search</a> |
        <a href="/profile">Profile</a> |
        <% if (currentUser) { %>
          <span>Logged in as <%= currentUser.username %></span> |
          <a href="/logout">Logout</a>
          <a href="/profile">Profile</a>
        <% } else { %>
          <a href="/register">Register</a> |
          <a href="/login">Login</a>
        <% } %>
      </nav>
    </header>

    <main>
      <section>
        <h2>Your Profile</h2>

        <% if (error) { %>
          <p class="error"><%= error %></p>
        <% } %>

        <% if (message) { %>
          <p class="success"><%= message %></p>
        <% } %>

        <p><strong>User ID:</strong> <%= user.id %></p>
        <p><strong>Role:</strong> <%= user.role %></p>
        <p><strong>Member since:</strong> <%= user.created_at %></p>
      </section>

      <section>
        <h3>Edit Profile</h3>

        <form method="post" action="/profile">
          <div>
            <label>Username</label>
            <input type="text" name="username" value="<%= user.username %>" required />
          </div>

          <div>
            <label>New Password (optional)</label>
            <input type="password" name="new_password" />
            <small>Leave blank to keep your current password.</small>
          </div>

          <button type="submit">Save Changes</button>
        </form>
      </section>

      <section>
        <h3>Danger Zone</h3>
        <p>
          Deleting your account will log you out and remove your profile. Your existing posts may be shown as "Anonymous".
        </p>
        <p>
          <a href="/profile/delete">Delete my account</a>
        </p>
      </section>
    </main>
  </body>
</html>
